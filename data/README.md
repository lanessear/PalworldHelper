# Breeding data

Replace `breeding.json` with the cleaned breeding data used by this project. Schema:

```json
[
  { "parent1": "Lamball", "parent2": "Cattiva", "child": "Example" }
]
```

The application deliberately does not download breeding information automatically. This keeps the project's calculations tied to the curated source chosen by the maintainer.
