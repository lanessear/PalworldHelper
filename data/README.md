# Reference data imports

This directory contains reviewed examples and import notes for reference data.

Do not commit real player saves or server exports here.

The current default breeding dataset lives at the repository root as `palworld_breeding_results_v1.0_2026-07-24.json` so it can be copied directly beside `PalworldHelper.exe` during publish.
